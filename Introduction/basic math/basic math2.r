x <- 12
y <- 5

#maximum
print(max(x, y))


#minimum
print(min(x, y))

#You can also use more than 2 numbers with the min and max functions - just separate them using commas.

# square root 

print(sqrt(64))