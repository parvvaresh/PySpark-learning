#We can use a negative index to skip an element in the vector.

vec <- c(1, 2, 3, 4, 5, 6, 7)
print(vec[-2])

#We can also specify a range to select only a subset of the elements.

print(vec[2:4])