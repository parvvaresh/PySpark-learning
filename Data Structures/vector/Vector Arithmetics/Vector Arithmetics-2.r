#Mean and median are measures of central tendency in statistics.

#R supports corresponding functions for vectors to find these values.
#The mean is the average of the data set and can be found using the mean()


x <- c(1, 2, 3, 5, 28, 4)

print(mean(x))

print(median(x))