#ou can add a new column to a dataframe by simply assigning it a vector of values.

grade <- data.frame(id = 1:2, grade = c(17, 18))

print(grade)


grade$name <- c("reza", "navid")

print(grade)

